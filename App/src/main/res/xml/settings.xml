<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="@string/opMode"
        android:summary="Set application's operation mode"
        android:defaultValue="1"
        android:entries="@array/opMode"
        android:entryValues="@array/opMode"
        android:dialogTitle="Choose operation mode"
        android:title="Mode" />

    <EditTextPreference
        android:key="@string/btLatencyMs"
        android:summary="Set minimal sleep interval (ms) on transmit BT2BT-packets"
        android:inputType="number"
        android:dialogTitle="Choose sleep interval"
        android:title="BT on transmit sleep interval" />

    <EditTextPreference
        android:key="@string/bt2NetFactor"
        android:summary="Set amount of BT-packets in one NET-send"
        android:inputType="number"
        android:dialogTitle="Choose BT2NET factor"
        android:title="BT2NET factor" />

    <CheckBoxPreference
        android:key="@string/btSinglePacket"
        android:defaultValue="false"
        android:summary="Set if we try not to use bufferization on BT data"
        android:title="No bufferization on BT" />

    <ListPreference
        android:key="@string/audioCodecType"
        android:summary="Set audiocodec for decoding/encoding of audio data"
        android:defaultValue="1"
        android:entries="@array/audioCodecType"
        android:entryValues="@array/audioCodecType"
        android:dialogTitle="Choose audiocodec"
        android:title="Audiocodec" />

</PreferenceScreen>